/**
 * Features Layer Barrel Export
 * Responsibilities: Lazy-loaded feature modules (dashboard, users, reviews, reports, analytics, audit-logs, settings, system, profile, auth).
 * Dependencies: Consumes shared components, domain services, and stores.
 */

export const FEATURES_LAYER_TOKEN = 'CDL_ADMIN_FEATURES';
