/**
 * Features Module Barrel Export
 * Purpose: Houses all lazy-loaded domain features (auth, dashboard, review, chat, reports, settings, profile, admin).
 * Responsibilities: Implements Smart Containers, feature route configurations, and domain user workflows.
 * Dependencies: Core Services, Signal Stores, Shared UI Components.
 */
export const FEATURES_MODULE_TOKEN = 'CDL_FEATURES_MODULE';
