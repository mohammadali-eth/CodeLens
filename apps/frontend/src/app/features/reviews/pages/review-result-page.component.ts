import { Component, ChangeDetectionStrategy } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * ReviewResultPageComponent
 * Purpose: Smart Container Page for Detailed AI Code Review Diagnostics.
 * Responsibilities: Metrics breakdown (Quality, Security, Maintainability), issue severity filters, line-by-line AI suggestions, and export controls.
 * Dependencies: Angular CommonModule, ReviewStore.
 */
@Component({
  selector: 'cdl-review-result-page',
  standalone: true,
  imports: [CommonModule],
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `
    <div class="review-result-container animate-fade-in">
      <header class="result-header">
        <div class="header-main">
          <div class="title-row">
            <h1>AI Code Review Diagnostics</h1>
            <span class="badge badge-success">Passed Audit</span>
          </div>
          <p class="subtitle">Analysis generated by Google Gemini Flash • 2.4s execution time</p>
        </div>
        <div class="header-actions">
          <button class="btn btn-secondary btn-sm">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
              <polyline points="7 10 12 15 17 10"/>
              <line x1="12" y1="15" x2="12" y2="3"/>
            </svg>
            <span>Export Report</span>
          </button>
          <button class="btn btn-primary btn-sm">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polyline points="23 4 23 10 17 10"/>
              <path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/>
            </svg>
            <span>Re-run Audit</span>
          </button>
        </div>
      </header>

      <!-- Key Score & Metrics Grid -->
      <section class="metrics-grid">
        <div class="score-card card-panel">
          <div class="score-ring-wrap">
            <div class="score-number">92</div>
            <span class="score-denom">/100</span>
          </div>
          <div class="score-meta">
            <h3>Overall Quality Score</h3>
            <p>Codebase meets enterprise production standards with minimal recommendations.</p>
          </div>
        </div>

        <div class="submetric-card card-panel">
          <div class="metric-icon success-icon">🔒</div>
          <div class="metric-info">
            <span class="metric-label">Security & Vulnerabilities</span>
            <span class="metric-status">0 Critical Risks Found</span>
          </div>
        </div>

        <div class="submetric-card card-panel">
          <div class="metric-icon warning-icon">⚡</div>
          <div class="metric-info">
            <span class="metric-label">Performance & Memory</span>
            <span class="metric-status">1 Minor Bottleneck</span>
          </div>
        </div>

        <div class="submetric-card card-panel">
          <div class="metric-icon info-icon">🛠️</div>
          <div class="metric-info">
            <span class="metric-label">Maintainability & Types</span>
            <span class="metric-status">98% Coverage</span>
          </div>
        </div>
      </section>

      <!-- AI Suggestions Feed -->
      <section class="suggestions-section card-panel">
        <div class="section-header">
          <h3>AI Diagnostic Findings (2 Items)</h3>
          <div class="filter-pills">
            <button class="pill active">All (2)</button>
            <button class="pill">Performance (1)</button>
            <button class="pill">Type Safety (1)</button>
          </div>
        </div>

        <div class="finding-card warning-finding">
          <div class="finding-header">
            <div class="finding-tag warning-tag">WARNING</div>
            <span class="file-location">main.ts : line 6</span>
          </div>
          <h4>Asynchronous Fetch Exception Handling</h4>
          <p class="finding-desc">
            Directly returning <code>fetch()</code> inside <code>analyzeCode</code> without a try/catch block can lead to unhandled promise rejections if network errors occur.
          </p>
          <div class="code-diff-box">
            <div class="diff-line remove">- return await fetch('/api/reviews', {{ '{' }} method: 'POST' {{ '}' }});</div>
            <div class="diff-line add">+ try {{ '{' }}</div>
            <div class="diff-line add">+   const response = await fetch('/api/reviews', {{ '{' }} method: 'POST' {{ '}' }});</div>
            <div class="diff-line add">+   return await response.json();</div>
            <div class="diff-line add">+ {{ '}' }} catch (error) {{ '{' }} throw new Error('Review request failed'); {{ '}' }}</div>
          </div>
        </div>

        <div class="finding-card info-finding">
          <div class="finding-header">
            <div class="finding-tag info-tag">OPTIMIZATION</div>
            <span class="file-location">main.ts : line 5</span>
          </div>
          <h4>Explicit Return Type Annotation</h4>
          <p class="finding-desc">
            Replace generic <code>Promise&lt;any&gt;</code> return type with a concrete TypeScript interface <code>Promise&lt;ReviewResult&gt;</code> for stronger static typing.
          </p>
        </div>
      </section>
    </div>
  `,
  styles: [`
    .review-result-container {
      padding: 1.75rem 2rem;
      max-width: 1400px;
      margin: 0 auto;
      width: 100%;
      box-sizing: border-box;
    }
    .result-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      margin-bottom: 1.75rem;
    }
    .title-row {
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }
    .title-row h1 {
      margin: 0;
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--text-primary);
      letter-spacing: -0.02em;
    }
    .subtitle {
      margin: 0.35rem 0 0;
      color: var(--text-muted);
      font-size: 0.85rem;
    }
    .header-actions {
      display: flex;
      gap: 0.65rem;
    }
    .metrics-grid {
      display: grid;
      grid-template-columns: 1.5fr 1fr 1fr 1fr;
      gap: 1.25rem;
      margin-bottom: 1.75rem;
    }
    .score-card {
      display: flex;
      align-items: center;
      gap: 1.25rem;
      padding: 1.25rem 1.5rem;
      background: var(--bg-surface);
      border: 1px solid var(--border-color);
      border-radius: var(--radius-lg);
      box-shadow: var(--shadow-sm);
    }
    .score-ring-wrap {
      display: flex;
      align-items: baseline;
      justify-content: center;
      width: 72px;
      height: 72px;
      border-radius: 50%;
      background: #ecfdf5;
      border: 2px solid #10b981;
      flex-shrink: 0;
      padding-top: 1rem;
      box-sizing: border-box;
    }
    .score-number {
      font-size: 1.6rem;
      font-weight: 800;
      color: #047857;
      line-height: 1;
    }
    .score-denom {
      font-size: 0.75rem;
      color: #10b981;
      font-weight: 600;
    }
    .score-meta h3 {
      margin: 0;
      font-size: 1rem;
      font-weight: 700;
      color: var(--text-primary);
    }
    .score-meta p {
      margin: 0.25rem 0 0;
      font-size: 0.775rem;
      color: var(--text-muted);
      line-height: 1.4;
    }
    .submetric-card {
      display: flex;
      align-items: center;
      gap: 1rem;
      padding: 1.25rem;
      background: var(--bg-surface);
      border: 1px solid var(--border-color);
      border-radius: var(--radius-lg);
      box-shadow: var(--shadow-sm);
    }
    .metric-icon {
      width: 40px;
      height: 40px;
      border-radius: var(--radius-md);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.2rem;
      flex-shrink: 0;
    }
    .success-icon { background: #ecfdf5; }
    .warning-icon { background: #fffbeb; }
    .info-icon { background: #eff6ff; }

    .metric-info {
      display: flex;
      flex-direction: column;
    }
    .metric-label {
      font-size: 0.775rem;
      color: var(--text-muted);
      font-weight: 500;
    }
    .metric-status {
      font-size: 0.9rem;
      font-weight: 700;
      color: var(--text-primary);
      margin-top: 0.15rem;
    }
    .suggestions-section {
      background: var(--bg-surface);
      border: 1px solid var(--border-color);
      border-radius: var(--radius-lg);
      padding: 1.5rem;
      box-shadow: var(--shadow-sm);
    }
    .section-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 1.25rem;
    }
    .section-header h3 {
      margin: 0;
      font-size: 1.05rem;
      font-weight: 700;
      color: var(--text-primary);
    }
    .filter-pills {
      display: flex;
      gap: 0.4rem;
    }
    .pill {
      background: var(--bg-app);
      border: 1px solid var(--border-color);
      padding: 0.3rem 0.65rem;
      border-radius: var(--radius-full);
      font-size: 0.75rem;
      color: var(--text-secondary);
      font-weight: 500;
      cursor: pointer;
    }
    .pill.active {
      background: var(--color-primary-light);
      color: var(--color-primary);
      border-color: var(--color-primary-border);
      font-weight: 600;
    }
    .finding-card {
      background: var(--bg-app);
      border: 1px solid var(--border-color);
      border-radius: var(--radius-md);
      padding: 1.125rem;
      margin-bottom: 1rem;
    }
    .finding-header {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      margin-bottom: 0.5rem;
    }
    .finding-tag {
      font-size: 0.65rem;
      font-weight: 800;
      padding: 0.15rem 0.45rem;
      border-radius: 4px;
      letter-spacing: 0.04em;
    }
    .warning-tag { background: #fef3c7; color: #b45309; }
    .info-tag { background: #e0f2fe; color: #0369a1; }
    .file-location {
      font-size: 0.775rem;
      font-family: var(--font-mono);
      color: var(--text-muted);
    }
    .finding-card h4 {
      margin: 0 0 0.35rem;
      font-size: 0.95rem;
      font-weight: 700;
      color: var(--text-primary);
    }
    .finding-desc {
      margin: 0;
      font-size: 0.825rem;
      color: var(--text-secondary);
      line-height: 1.5;
    }
    .finding-desc code {
      background: #e2e8f0;
      padding: 0.1rem 0.3rem;
      border-radius: 4px;
      font-family: var(--font-mono);
      font-size: 0.775rem;
      color: var(--text-primary);
    }
    .code-diff-box {
      margin-top: 0.85rem;
      background: #0f172a;
      border-radius: var(--radius-sm);
      padding: 0.75rem 1rem;
      font-family: var(--font-mono);
      font-size: 0.8rem;
    }
    .diff-line {
      line-height: 1.6;
    }
    .diff-line.remove { color: #f87171; background: rgba(239, 68, 68, 0.1); }
    .diff-line.add { color: #34d399; background: rgba(16, 185, 129, 0.1); }

    @media (max-width: 1024px) {
      .metrics-grid {
        grid-template-columns: 1fr;
      }
    }
  `],
})
export class ReviewResultPageComponent {}
