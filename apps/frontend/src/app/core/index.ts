/**
 * Core Module Barrel Export
 * Purpose: Contains singleton core services, guards, interceptors, and low-level infrastructural primitives.
 * Responsibilities: Single instance application-wide infrastructure (Auth Guards, HTTP Interceptors, Token Storage).
 * Dependencies: Angular Core, HttpClient, Router.
 */
export const CORE_MODULE_TOKEN = 'CDL_CORE_MODULE';
