/**
 * Models Module Barrel Export
 * Purpose: Contains reusable TypeScript models, DTO contracts, enum types, and API payload structures.
 * Responsibilities: Enforces strict compile-time typing for HTTP responses, User profiles, JWT tokens, and error payloads.
 * Dependencies: @codelens/shared-dto (where applicable).
 */
export const MODELS_MODULE_TOKEN = 'CDL_MODELS_MODULE';
