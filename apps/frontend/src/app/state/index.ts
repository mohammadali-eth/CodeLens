/**
 * State Module Barrel Export
 * Purpose: Global Signal Store management (AuthStore, ThemeStore, LoadingStore, NotificationStore).
 * Responsibilities: Holds application reactive state, computes derived state (`computed()`), and handles side effects (`effect()`).
 * Dependencies: Angular Signals.
 */
export const STATE_MODULE_TOKEN = 'CDL_STATE_MODULE';
